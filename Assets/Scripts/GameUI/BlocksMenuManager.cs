using UnityEngine;

public class BlocksMenuManager : MonoBehaviour {
  private readonly float SPACING = 10f;
  private readonly float PADDING_TOP = 10f;
  private readonly float PADDING_RIGHT = 10f;
  private readonly float PADDING_BOTTOM = 10f;
  private readonly float PADDING_LEFT = 10f;
}
